import { PropertyToWordPipe } from './property-to-word.pipe';

describe('PropertyToWordPipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyToWordPipe();
    expect(pipe).toBeTruthy();
  });
});
